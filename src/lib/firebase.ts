// Firebase configuration - Demo mode only
// Firebase is disabled for frontend testing

let app = null;
let auth = null;
let db = null;
let googleProvider = null;

console.warn('Firebase disabled. Running in demo mode.');

export { app, auth, db, googleProvider };
